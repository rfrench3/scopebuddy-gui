Image size: 1920x1440 (set forced window size on KDE to 960x720, take screenshots with 200% desktop scaling)

If the titlebar is integrated into the UI (like Lutris), include it. Otherwise (like Heroic), do not.

Do not include window shadows.
